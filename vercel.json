{
    "builds": [
      {
        "src": "app.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/server/routes/$1" },
      { "src": "/(.*)", "dest": "/app.js" }
    ]
  }
